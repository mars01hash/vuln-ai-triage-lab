# Optional v3 dependencies.
# Install only when you want neural embeddings, Chroma memory, or real LLM calls.
-r requirements.txt
sentence-transformers>=3.0.0
chromadb>=0.5.0
openai>=1.40.0
langgraph>=0.2.0
mlflow>=2.14.0
evidently>=0.4.30
